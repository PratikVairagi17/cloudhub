# cloudhub

Python FastAPI wrapper for the `cloud-engineer-hub.html` UI.

## Run locally

- Create a virtualenv (optional but recommended) and install dependencies:

```bash
pip install -r requirements.txt
```

- Start the FastAPI app with uvicorn:

```bash
uvicorn main:app --reload
```

Then open `http://localhost:8000` in your browser.
